#include <opencv2/opencv.hpp>
#include <iostream>

using namespace std;
using namespace cv;

int main() {
    string input;
    cout << "Enter query image name: ";
    cin >> input;
    
    
    // imshow("Query", finger_print);
    // imshow("Best_matching", adaptive_1);

    waitKey(0);

}